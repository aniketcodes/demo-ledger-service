services:
  ledger-service:
    build: .
    ports:
      - "8003:8003"
    environment:
      - OTEL_EXPORTER_OTLP_ENDPOINT=otel-collector:4317
      - OTEL_SERVICE_NAME=demo-ledger-service
    networks:
      - demo-network

networks:
  demo-network:
    external: true
